import { AmountColorPipe } from './amount-color.pipe';

describe('AmountColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountColorPipe();
    expect(pipe).toBeTruthy();
  });
});
